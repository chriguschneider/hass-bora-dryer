name: Claude (@mention)

# Write "@claude ..." in an issue or PR comment and Claude works that item.
# Draft PRs only, never the default branch. Requires CLAUDE_CODE_OAUTH_TOKEN.

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

jobs:
  claude:
    # Only the owner / collaborators can trigger this. Comment events run with
    # repo secrets even for non-collaborators, so gate on author role.
    if: >-
      contains(github.event.comment.body, '@claude') &&
      contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'),
               github.event.comment.author_association)
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
    concurrency:
      group: claude-mention-${{ github.event.issue.number || github.event.pull_request.number }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Pick model
        id: model
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          n="${{ github.event.issue.number || github.event.pull_request.number }}"
          bash .github/scripts/pick-model.sh "$n" >> "$GITHUB_OUTPUT"
      - uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          claude_args: "--model ${{ steps.model.outputs.model }} --max-turns 20 --dangerously-skip-permissions"
